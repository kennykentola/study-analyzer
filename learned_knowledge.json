
{
    "what is compiler": "A compiler is a computer program that translates source code written in one language (the source language) into another language (the target language). It's typically used to convert a high-level programming language into machine code (or bytecode) that a computer can directly execute."
}
